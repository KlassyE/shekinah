---
deployment:
  tasks:
    # Repository checkout: /home/calidadadmin/repositories/shekinah/dist
    # Addon domain Document Root: /home/calidadadmin/repositories/shekinah/dist/dist
    # The committed production build is served directly, so no copying is needed.
    - export DEPLOYPATH=/home/calidadadmin/repositories/shekinah/dist/dist
    - /bin/true
