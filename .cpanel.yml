---
deployment:
  tasks:
    # The site is served directly from this repo's dist/ folder
    # (addon domain Document Root = /home/calidadadmin/shekinah.ac.ug/dist).
    # cPanel checks out the latest commit automatically, so no copying is needed.
    - export DEPLOYPATH=/home/calidadadmin/shekinah.ac.ug/dist
    - /bin/true
