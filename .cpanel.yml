---
deployment:
  tasks:
    - export DEPLOYPATH=/home/calidadadmin/shekinah.ac.ug
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -Rf dist/. $DEPLOYPATH/
    - /bin/cp -f dist/.htaccess $DEPLOYPATH/ 2>/dev/null || true
